name: 'data__bargin_sort'
version: '1.0.0'
profile: 'data__bargin_sort'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Collection radius. Widened from 50 km to the full 50 miles (80.5 km) for a
# multi-week aggregation phase: cast wide first, then decide from the data what
# is actually worth targeting. `lots_within_50km` deliberately keeps the tighter
# screen, so the two questions — what exists nearby, and what is worth driving
# to — stay separable.
vars:
  radius_km: 80.5

models:
  data__bargin_sort:
    # Silver: raw JSONB flattened into typed columns, deduplicated to one
    # row per lot per run. Tables rather than views because silver_enhanced
    # joins across them and the JSONB extraction is not cheap to repeat.
    silver:
      +schema: silver
      +materialized: table
    # Silver enhanced: silver plus derived attributes that need a join to
    # reference data (postal centroids today, Claude evals later).
    silver_enhanced:
      +schema: silver_enhanced
      +materialized: table

seeds:
  data__bargin_sort:
    +schema: reference
    postal_centroids:
      +column_types:
        country_code: varchar(2)
        postal_code: varchar(10)
        place_name: varchar(180)
        admin1_code: varchar(100)
        latitude: double precision
        longitude: double precision
